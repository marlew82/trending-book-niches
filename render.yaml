services:
  - type: web
    name: book-trends-analyzer
    env: python
    runtime: python-3.11.0
    buildCommand: "pip install -r requirements.txt && python build.py"
    startCommand: "gunicorn app:app"
